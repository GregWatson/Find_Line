Trying to extract straight line segments from an image of a jigsaw piece.
The image must be converted to black and white, then run edge detect to yield an image consisting of only the points on the edges.
Then pass that to this in order to try to extract line segments that can then be used to try to identify corners. Ah ha!
